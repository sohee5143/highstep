# 분기 시스템 테스트 가이드

## 📝 테스트 개요

분기 시스템 마이그레이션 후 다음 항목들을 테스트합니다.

## ✅ 단위 테스트 (quarterTests.ts)

프로젝트에 `src/utils/quarterTests.ts` 파일이 포함되어 있습니다. 이 파일에는 다음과 같은 테스트가 포함됩니다:

```bash
# 테스트 실행 (필요시)
npm test -- quarterTests
```

### 테스트 항목
1. **월 → 분기 매핑**: 각 월이 올바른 분기로 변환되는지 확인
2. **분기 키 파싱**: "2026_Q1" 형식 파싱 검증
3. **분기 네비게이션**: 이전/다음 분기 계산 정확성
4. **단일 분기 상태 계산**: 4/5 규칙 검증
5. **2-분기 규칙**: 연속 두 분기의 규칙 검증
6. **전체 상태 결정**: 최종 출석 상태 계산

## 🌐 통합 테스트

### 1. /list 페이지 분기 선택 테스트

**테스트 목표**: 분기 선택 드롭다운이 올바르게 작동하는가?

**단계**:
1. `/list` 페이지 접속
2. 헤더의 "분기 선택" 드롭다운 확인
3. 다양한 분기 선택:
   - 2026년 1분기
   - 2026년 2분기
   - 2025년 4분기
   - 기타 분기들
4. 각 분기 선택 시:
   - 페이지 제목이 업데이트되는지 확인
   - 출석 현황 데이터가 변경되는지 확인
   - 로딩 상태가 표시되는지 확인

**예상 결과**:
- 선택한 분기에 맞는 정보가 표시됨
- 분기별 출석 현황이 정확함

### 2. /user 페이지 이전 분기 테스트

**테스트 목표**: 개인 페이지에서 현재 분기 + 이전 분기 정보가 표시되는가?

**단계**:
1. `/user` 페이지 접속
2. 부원 이름 검색
3. 결과 화면에서:
   - 현재 분기 정보 표시 확인
   - "이전 분기 기록" 섹션이 존재하는지 확인
4. "이전 분기 기록" 토글:
   - 펼침 버튼 클릭
   - 이전 분기 출석 정보 표시 확인
   - 접힘 버튼 클릭 시 정보 숨김 확인

**예상 결과**:
- 현재 분기: 2026년 Q1 정보 표시
- 이전 분기: 2025년 Q4 정보 표시
- 아코디언 토글 정상 작동

### 3. 분기 경계 테스트

**테스트 목표**: 1월(분기 경계)이 올바르게 처리되는가?

**테스트 데이터**:
- 현재 날짜가 2025-01-15일 경우
  - 현재 분기: 2024년 Q4 (1월은 이전 년도 Q4)
  - 이전 분기: 2024년 Q3

**테스트 시나리오**:
1. 시스템 시간을 2월로 설정하고 테스트
2. 시스템 시간을 1월로 설정하고 테스트
3. 각 경우에 올바른 분기가 선택되는지 확인

### 4. API 테스트

**테스트 목표**: 백엔드 API가 분기 파라미터를 올바르게 처리하는가?

```bash
# cURL로 API 테스트
# 기본 (현재 분기)
curl "http://localhost:3000/api/attendance-summary"

# 특정 분기
curl "http://localhost:3000/api/attendance-summary?year=2026&quarter=1"
curl "http://localhost:3000/api/attendance-summary?year=2025&quarter=4"
```

**확인 사항**:
- 응답 형식: JSON 배열
- 각 레코드에 `year`, `quarter`, `quarterKey` 포함 확인
- 데이터의 정확성 확인

### 5. 4/5 규칙 테스트

**테스트 목표**: 출석 규칙이 올바르게 적용되는가?

**시나리오 1: 단일 분기**
```
필요 출석: 5회
현재 출석: 5회 → 'full' (완료)
현재 출석: 4회 → 'minus_one' (부족 1회)
현재 출석: 3회 이하 → 'X' (미충족)
```

**시나리오 2: 2-분기 규칙**
```
현재 분기: 5회, 이전 분기: 5회 → 'full' (모두 완료)
현재 분기: 4회, 이전 분기: 5회 → 'minus_one' (한 분기 부족 1회)
현재 분기: 4회, 이전 분기: 4회 → 'X' (양 분기 모두 미충족)
현재 분기: 5회, 이전 분기: 4회 → 'minus_one'
```

### 6. 데이터 일관성 테스트

**테스트 목표**: 이전 분기 데이터가 변경되지 않는가?

**단계**:
1. 부원 이름 검색
2. 현재 분기 정보 기록
3. 이전 분기 정보 기록
4. 24시간 후 같은 부원 다시 검색
5. 이전 분기 정보가 변경되지 않았는지 확인

## 📊 성능 테스트

### 응답 시간 테스트

```bash
# /list 페이지 로딩 시간 측정 (분기 선택 시)
time curl "http://localhost:3000/api/attendance-summary?year=2026&quarter=1"

# 기대값: 200-500ms 이내
```

### 데이터베이스 인덱스 확인

```sql
-- 인덱스 사용 확인
EXPLAIN ANALYZE
SELECT * FROM member_season_progress 
WHERE year = 2026 AND quarter = 1;
```

## 🐛 문제 해결

### 문제 1: 분기 선택이 작동하지 않음
- 브라우저 콘솔에서 에러 확인
- `fetchAttendanceSummaryByQuarter` 함수 호출 확인
- 네트워크 탭에서 API 요청 확인

### 문제 2: 이전 분기 정보가 표시되지 않음
- `getPreviousQuarter` 함수가 올바른 값을 반환하는지 확인
- 이전 분기에 대한 데이터가 DB에 있는지 확인

### 문제 3: 출석 상태가 올바르지 않음
- `quarterTests.ts`의 테스트 케이스 실행
- `calculateTwoQuarterStatus` 함수 검증
- DB의 status 값 확인

## ✅ 최종 체크리스트

- [ ] 분기 선택 드롭다운 작동 확인
- [ ] 현재/이전 분기 데이터 조회 확인
- [ ] 이전 분기 아코디언 토글 작동
- [ ] 분기 경계 처리 확인 (1월)
- [ ] 4/5 규칙 적용 확인
- [ ] API 응답 시간 확인
- [ ] 데이터 일관성 확인
- [ ] 모바일/데스크탑 UI 확인
